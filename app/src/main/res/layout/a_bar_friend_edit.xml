<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        android:layout_width="48dp"
        android:layout_height="match_parent"
        android:layout_gravity="start|bottom"
        android:id="@+id/imgButtonSave"
        android:background="@color/text_days_left_color_urgently"
        android:layout_weight="0"
        android:contentDescription="@string/save_button_title" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:textSize="@dimen/standard_text_size"
        android:text="@string/add_contact_layout_title"
        android:textAlignment="center"
        android:id="@+id/labelFriendEdit"
        android:textColor="@android:color/black"
        android:layout_weight="1"
        android:gravity="center" />

    <ImageButton
        android:layout_width="48dp"
        android:layout_height="match_parent"
        android:background="@color/text_days_left_color_urgently"
        android:id="@+id/imgButtonDiscard"
        android:layout_weight="0"
        android:contentDescription="@string/cancel_button_title" />

</LinearLayout>